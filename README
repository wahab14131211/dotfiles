##This file contains some extra commands which will be needed to setup these dotfiles.
#GNU stow command to softlink dotfiles into the home directory:
stow */ -t ~

#command to download vimplug for vim plugins (also you need to run :PlugInstall in vim):
curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
